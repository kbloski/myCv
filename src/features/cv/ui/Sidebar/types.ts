export type SidebarProps = {
    children?: React.ReactNode;
};

export type SidebarContainerProps = {
    children?: React.ReactNode;
    mode?: "light";
    className?: string;
};

export type SidebarContainerContentProps = {
    children: React.ReactNode;
}
